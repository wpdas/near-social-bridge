1 - [issue] Fazer o app voltar para mesma pagina com o mesmo estado apos o refresh (3 x press R)
1 - [issue] Quando vou pra outra tela, a tela atual da erro caso tenha propriedades nela que
esta sendo utilizado;

- Criar recurso para saber a altura da tela (subtraindo o espaço usado pelo Near View)
Atualmente o tamanho usado é 96px [Impossível no momento já que o Widget nao tem acesso ao Window.innerHeight]
1 - Usar Storage para sessionStorage/persistStorage?
1 - Implantar um jeito de acessar a Discovery API localmente
- Arrumar sessionStorage usando Storage da Near social. [Náo é recomendado usar o Storage API porque
ele seta globalmente dentro do Widget, todos os usuarios seriam afetados]

- Criar Link para o navigation: ver arquivo TaskToolbar linha 17 e 88 (menuItems)
- Melhorar o README.me
- Colocar exemplo de todos os hooks no README.md
- Colocar no README sobre o Navigator setar o cumprimento do conteúdo da tela no iframe automaticamente
- Criar testes
- Usar esses dois recursos para melhor desenvolvimento e deploy: 
https://github.com/FroVolod/near-social%20https://github.com/near/near-vscode
- Nota para contribuidor:
Usar algum editor de código como o VSCode para editar a parte do "code", salvar o arquivo
core.js e também o componente.

PRIORIDADE:
- Implantar session storage usando Storage API e atualizar documentacao
- Checar o porque do tamanho do arquivo do bundle com a lib está tao grande (@faker-js/faker)
-- switched from @faker-js/faker to @ngneat/falso
- As vezes o tamanho do iframe nao atualiza quando o app é inicializado

- Added message concurrency control - DONE
- `request` now has a try-again mechanic - DONE
- `request` now is using reject method - DONE
- Tentar usar uma lib gratuita para export o app (tentar usar configuracao de CORS usando Next/Node)
: Duas solucoes possiveis: Usar Next App ou Nodemon (buildar o projeto a cada alteracao, isso vai ser demorado)
=> Melhor opcao é implantar um app Next (passar os headers requests que o localtunnel pede) e testar.
=> Se funcionar, mostrar o caminho de configuracao no README
- A solucao acima é mais cara que usar NGrok, então, talvez a solucao seja criar uma VM simulada localmente, injentando
a API de forma fake, elas devem ter a opcao de passar mocks

REFS:
[ngrok de graça (ver como desativar msg de boas vindas)] - Melhor que localtunnel
https://github.com/localtunnel/localtunnel
https://www.appsloveworld.com/reactjs/100/11/how-to-set-headers-for-a-react-app-created-using-create-react-app
https://www.npmjs.com/package/nodemon
https://www.softwaretestinghelp.com/ngrok-alternatives/



NOTAS:
- É melhor ter o core do Iframe pq da pra usar useEffect, o que não é possivel usando o VM.
recursos como criar concorrencia de envio de mensagens por exemplo, falham porque
o "state" não se atualiza dentro de outro blocos de função.
NOTAS FIM

News (publicado na 1.0.0-beta8):
